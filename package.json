{
  "name": "synthica-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Synthica dashboards (React + Vite SPA at the repo root) + journal backend.",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n backend,app -c blue,magenta \"npm run dev:backend\" \"npm run dev:app\"",
    "dev:backend": "npm --prefix backend start",
    "dev:app": "vite",
    "build": "vite build && cp docs/index.html docs/404.html && touch docs/.nojekyll",
    "preview": "vite preview",
    "install:all": "npm install && npm --prefix backend install",
    "postinstall": "npm --prefix backend install",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "concurrently": "^9.0.1",
    "vite": "^5.4.8"
  }
}
